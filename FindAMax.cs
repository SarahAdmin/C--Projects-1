using System; 
namespace NumWork2 { 
  class MaxNumbers { 
    static int FindAMax(int num1, int num2) { 
      return Math.Max(num1,num2);
    }
    static void (string[] args) { 
      Console.WriteLine(FindAMax(48,285);
    }
  }
}
